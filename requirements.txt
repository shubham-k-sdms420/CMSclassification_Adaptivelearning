# CMS Complaint Classification API (CMS_RoBerta)
# Requirements for XLM-RoBERTa + hybrid ensemble (SGDClassifier)

# Core ML & Transformers
torch>=2.0.0
transformers>=4.36.0

# API Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
requests>=2.31.0

# Adaptive Classifier (SGDClassifier + TF-IDF)
numpy>=1.24.0
scikit-learn>=1.3.0
